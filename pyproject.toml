[project]
name = "gaming-analytics-ai-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"

# dependencies
dependencies = [
    "chromadb>=1.0.4",
    "openai>=1.73.0",
    "pydantic>=2.11.3",
    "python-dotenv>=1.1.0",
    "tavily-python>=0.5.4",
    "pysqlite3",
    "langfuse>=4.15.2",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
