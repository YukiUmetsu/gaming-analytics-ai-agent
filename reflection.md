

# Local setup
1. create .env file by coping sample.env file and create .env file, then edit API keys.
2. run below.
```CLI
# install dependencies
uv sync

# activate virtual environment (venv)
source .venv/bin/activate
```